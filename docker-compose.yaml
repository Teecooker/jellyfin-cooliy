services:
  headscale:
    image: headscale/headscale:latest
    container_name: headscale
    restart: unless-stopped
    command: ["serve", "--config", "/etc/headscale/config.yaml"]
    volumes:
      - ./data:/data   # Nur noch das Daten-Volume
    cap_add:
      - NET_BIND_SERVICE